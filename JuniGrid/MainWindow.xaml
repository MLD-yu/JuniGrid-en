<Window x:Class="JuniGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JuniGrid.Components"
        xmlns:blazor="clr-namespace:Microsoft.AspNetCore.Components.WebView.Wpf;assembly=Microsoft.AspNetCore.Components.WebView.Wpf"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="JuniGrid — Your Junimo helper"
        Height="576" Width="1024"
        MinHeight="450" MinWidth="800"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="False"
        Background="#0B1510"
        ShowInTaskbar="True"
        Icon="Assets/junigrid-logo.ico">

    <!--
      AllowsTransparency must stay FALSE — WebView2 is a child HWND
      and layered/transparent windows cannot host it.
      Rounded corners come from DWM (see OnSourceInitialized).
    -->
    <!--
      CaptionHeight="0" is the key: the title bar is drawn entirely by Blazor itself (jg-titlebar);
      dragging uses a JS callback into DragMove (see TitleBar.razor + junigridJs.enableWindowDrag).
      Previously CaptionHeight="40" made the top 40px act as the system title bar,
      swallowing clicks on all Blazor buttons (minimize/close) and the built-in browser toolbar.
    -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="6"
                      CornerRadius="0"
                      GlassFrameThickness="0"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Grid>
        <blazor:BlazorWebView x:Name="blazorWebView"
                              HostPage="wwwroot/index.html"
                              Services="{StaticResource services}">
            <blazor:BlazorWebView.RootComponents>
                <blazor:RootComponent Selector="#app"
                                      ComponentType="{x:Type local:Routes}" />
            </blazor:BlazorWebView.RootComponents>
        </blazor:BlazorWebView>

        <!-- ===== Built-in Nexus browser: overlay inside the main window =====
             Free users click Mod Manager Download here; the nxm:// link is intercepted
             and installed automatically. Uses a separate WebView2 environment (its own
             user data directory) to avoid state conflicts with the Blazor WebView (0x8007139F). -->
        <!-- Embedded content area (not a full-window overlay): leaves room for the 40px title bar
             + 232px sidebar + 46px top nav, with page padding all around, so it looks like a browser
             panel embedded in the Mods page content area. -->
        <!-- The built-in Nexus browser overlay has been removed: all links now open in the system browser -->
    </Grid>
</Window>
